# Local environment configuration (credentials)
# Copy this file as .env and fill in real values; .env is gitignored.

# Database
DB_HOST=localhost
DB_NAME=r4_survey
DB_USER=root
DB_PASS=
DB_CHARSET=utf8mb4
